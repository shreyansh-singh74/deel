# Optimized requirements for Railway deployment
# Note: PyTorch CPU is installed separately in nixpacks.toml to reduce build time

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Data processing
pandas==2.1.3
numpy>=1.24.0,<2.0.0

# Text processing
rapidfuzz==3.14.3
python-Levenshtein>=0.21.1
unidecode>=1.3.7

# ML/AI (PyTorch installed separately via nixpacks.toml)
transformers>=4.30.0,<5.0.0
sentence-transformers>=2.3.0

# Utilities
requests==2.31.0

